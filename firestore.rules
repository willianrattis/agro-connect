rules_version = '2';

// Phase 1 (bootstrap): deny all reads and writes by default.
// Hardened to owner-based rules in Phase 7 (see docs/SPEC.md section 9).
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
