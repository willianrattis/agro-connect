rules_version = '2';

// Phase 1 (bootstrap): deny all reads and writes by default.
// Will be tightened in Phase 6 once auth and data model are implemented.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
